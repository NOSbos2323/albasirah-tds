# Cloudflare Pages configuration
# هذا الملف يُستخدم بواسطة wrangler CLI للنشر

name = "albasirah-tds"
compatibility_date = "2024-09-25"
pages_build_output_dir = "public"

# ملاحظة: هذا للنشر عبر wrangler. لو استخدمت Git integration على Cloudflare dashboard،
# هذا الملف يُتجاهل (يستخدم Cloudflare إعداداته الخاصة).

[vars]
ENVIRONMENT = "production"
